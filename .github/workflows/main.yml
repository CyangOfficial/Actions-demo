name: GitHub Action Demo
on: 
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Master
        uses: actions/checkout@v1
        
      - name: Cat text
        run: | 
          'cat dsatext'
      
